import { FieldGeneratorDirective } from './field-generator.directive';

describe('FieldGeneratorDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldGeneratorDirective();
    expect(directive).toBeTruthy();
  });
});
